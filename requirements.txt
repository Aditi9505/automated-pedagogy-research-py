transformers
torch
```

---

### **Step 3: How to Add and Submit to GitHub**

Once you have these files in your project directory, follow these steps to upload them to your new GitHub repository.

1.  **Initialize Git:**
    ```bash
    git init
    ```
2.  **Stage all files:**
    ```bash
    git add .
    ```
3.  **Commit the files:**
    ```bash
    git commit -m "feat: Initial commit of the research project"
    ```
4.  **Connect to your GitHub repository:**
    ```bash
    git remote add origin https://github.com/Aditi9505/Workshop-ui-ux-enhancement.git
    ```
5.  **Rename your default branch:**
    ```bash
    git branch -M main
    ```
6.  **Push your code:**
    ```bash
    git push -u origin main
    
